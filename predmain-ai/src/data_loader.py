"""Legacy wrapper kept for backwards compatibility."""

from __future__ import annotations

print("Use `python src/main.py ingest --csv data/raw/sample_sensor_data.csv` to run the new ingestion flow.")
